/**
 * Enumeration of pet colors used across the model.
 * Intended for consistent color typing in {@link AbstractPet} and subclasses.
 */
public enum Color {
    BLACK,
    WHITE,
    BROWN,
    ORANGE,
    YELLOW,
    GREEN,
    GRAY,
    SPOTTED,
    MULTICOLOR
}
